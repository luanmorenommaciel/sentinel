# Copy to .env (gitignored) and fill in. docker compose substitutes these.

# HyperDX / ClickStack ingestion API key (sent as the OTLP 'authorization' header).
# Get it from the HyperDX UI at http://localhost:8080 → Team Settings.
OTELGEN_OTLP_API_KEY=
